# LawnDart.Authorization.AspNetCore

HTTP claims → `AuthorizationContext` adapter.

```csharp
services.AddLawnDartAuthorization();
services.AddHttpAuthorizationContext();
```

Optional. `LawnDart.AspNetCore` maps commands without this package.
