# LawnDart.Authorization.AspNetCore

HTTP claims → `AuthorizationContext`.

```csharp
services.AddHttpAuthorizationContext();
```

Additive: Core auth contracts stay in `LawnDart`. See [AUTHORIZATION.md](../AUTHORIZATION.md).
